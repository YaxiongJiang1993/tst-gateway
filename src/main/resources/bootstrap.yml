spring:
  application:
    name: tst-gateway
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        namespace: public
      config:
        namespace: public
        file-extension: yml
        group: DEFAULT_GROUP
